'use strict';

export const getTimeSinceStartingCoding = function getTimeSinceStartingCoding() {
  const startingDate = new Date('2018-09-01');
  const now = Date.now();
  return {
    years: 1,
    months: 8
  };
};
